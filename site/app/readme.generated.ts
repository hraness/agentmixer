// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "xcb";
export const readmeLead = "xcb (Excalibur) is a local, terminal-first workspace for coding agents. Bring your accounts, choose your models, and shape the interface around your work. Small, composable extensions handle usage, continuation, and context management; the kernel keeps sessions, credentials, and execution under explicit custody.";
export const readmeHtml = "<h1 id=\"xcb\">xcb</h1>\n<p>xcb (Excalibur) is a local, terminal-first workspace for coding agents. Bring\nyour accounts, choose your models, and shape the interface around your work.\nSmall, composable extensions handle usage, continuation, and context management;\nthe kernel keeps sessions, credentials, and execution under explicit custody.</p>\n<p><a href=\"https://xcb.dev\">Project site</a> · <a href=\"#native-xcb\">Native interface</a> · <a href=\"#standalone-package\">Compatibility package</a></p>\n<h2 id=\"native-xcb\">Native xcb</h2>\n<p>xcb is the new name and direction for AgentMixer. The native Rust kernel and\nRatatui interface are in development. The published\n<code>@hraness/xcb</code> package below is the retained TypeScript compatibility\nrelease, not a release of the new native interface. Existing AgentMixer\naccount state, transcripts, imports, and release coordinates are not silently\nrenamed or overwritten; see <a href=\"#migrating-from-agentmixer\">migration</a>.</p>\n<p>The native design is deliberately local. There is no required daemon, cloud\naccount, synchronization service, or task-shape router. Pick a provider and model,\nset your favorites, and keep working in one terminal.</p>\n<ul>\n<li><strong>Accounts and subscriptions.</strong> Named accounts, compact quota windows, observed\ntoken velocity, and estimated runway. Missing or stale quota stays unknown;\ntoken counts are not treated as a provider's billing statement.</li>\n<li><strong>A quiet session view.</strong> Your latest input and the selected model stay visible.\nThinking and older responses collapse; tool activity stays out of the way.\nSubagent state, a semantic status badge, and a live usage meter carry the signal.</li>\n<li><strong>Userspace panes.</strong> <code>/pane</code> selects a preset or a local declaration. Edit or\ngenerate a pane without rebuilding the terminal. Reload validates the whole\ncandidate and retains the last working view on error.</li>\n<li><strong>Optional behavior.</strong> Auto-continue and Gobstopper context management default\non, with bounded execution and explicit off switches. Hooks extend named\nlifecycle events; a view is never permission to execute a hook.</li>\n<li><strong>Simple model choice.</strong> Favorites precede the rest of the observed provider\ncatalog. Limit failover uses your order and a settled handoff, never a blind\nreplay of an uncertain turn.</li>\n<li><strong>aiCharts integration.</strong> Reuse its local measurement formats. Publishing is\nseparate and opt-in at idle boundaries; no session text or credentials belong\nin numeric exports. The upstream usage service is not yet live-qualified.</li>\n</ul>\n<p>The implementation references Pi's malleable extensions, Codex's native terminal\nfoundation, Ghostget's named capability boundaries, and Oompa's usage and recovery\nlessons. It does not inherit Oompa's cloud control plane or make promises about\nprovider features that have not been admitted.</p>\n<h3 id=\"install-the-native-binary\">Install the native binary</h3>\n<p>The <code>xcb</code> CLI builds with Rust 1.97.1 and works on macOS and Linux. A release\ntarball for your OS and architecture is attached to every <code>v&lt;version&gt;</code> GitHub\nRelease as <code>xcb-&lt;version&gt;-&lt;os&gt;-&lt;arch&gt;.tar.gz</code> plus a <code>.sha256</code> checksum.</p>\n<p>Install the latest release to <code>~/.local/bin</code>:</p>\n<pre><code class=\"language-sh\">XCB_VERSION=0.1.0 ./scripts/install-native.sh\n</code></pre>\n<p>Build and install from source instead:</p>\n<pre><code class=\"language-sh\">./scripts/install-native.sh\n</code></pre>\n<p>Generate shell completions:</p>\n<pre><code class=\"language-sh\">xcb completions bash &gt; /path/to/completions/xcb.bash\nxcb completions zsh  &gt; /path/to/completions/_xcb\nxcb completions fish &gt; /path/to/completions/xcb.fish\n</code></pre>\n<h3 id=\"devin-model-modes\">Devin model modes</h3>\n<p>Fixed models, Adaptive, and Fusion are different selections. Adaptive delegates\nrouting to Devin. Fusion pairs a frontier lead with a sidekick; its supported\npairings have provider-issued model identifiers. xcb must select those exact\nobserved identifiers, not synthesize combinations or assign a single capability\nscore to every pairing. The selected mode and any provider-reported serving\nmodels remain separate usage dimensions. Promotional prices are not a durable\nsubscription contract.</p>\n<p>The source behavior was checked against Devin CLI 3000.10.31's model catalog\nand the public <a href=\"https://docs.devin.ai/cli/adaptive\">Adaptive documentation</a> and\n<a href=\"https://cognition.com/blog/local-fusion\">Fusion architecture</a>. Catalog presence\nproves a selection exists; native adapter admission and live behavior remain\nseparate checks.</p>\n<h2 id=\"typescript-compatibility\">TypeScript compatibility</h2>\n<p>The retained application package provides:</p>\n<ul>\n<li>A Codex/Claude/Devin adapter interface with explicit runtime qualification.</li>\n<li>Shared SQLite account custody, generation fencing and process-aware recovery.</li>\n<li>A tool broker bound to one workspace and run, with closed file, public-web and\nmessaging operations. There is no shell, executable or arbitrary RPC operation.</li>\n<li>Strict classifier output validation and selection from a fresh, host-observed\nmodel catalog. The cost comparison uses a 2,000-input / 128-output-token request.</li>\n</ul>\n<p><code>src/index.ts</code> exports the complete current interface. <code>createPublicWeb()</code> provides bounded public HTTPS GETs with address pinning, redirect checks, no ambient authentication, a 15-second deadline, and a 256 KiB maximum text response. Run <code>bun test</code> from the repository root.</p>\n<h2 id=\"install\">Install</h2>\n<p>Bun 1.3.14 or newer, or Node 22.13 or newer, is required. Add the canonical,\nversioned GitHub archive to a Bun project:</p>\n<pre><code class=\"language-sh\">bun add --exact --ignore-scripts https://github.com/hraness/xcb/releases/download/v0.3.0/hraness-xcb-0.3.0.tgz\n</code></pre>\n<p>The same release is mirrored to <a href=\"https://www.npmjs.com/package/@hraness/xcb\">npm</a>:</p>\n<pre><code class=\"language-sh\">npm install --save-exact --ignore-scripts @hraness/xcb@0.3.0\n</code></pre>\n<h2 id=\"standalone-package\">Standalone package</h2>\n<p><code>npm pack</code> produces a self-contained tarball after <code>bun scripts/build-dist.ts</code> emits <code>dist/</code>: the <code>files</code> allowlist ships\nonly <code>dist</code> (a bundled ESM entry plus TypeScript declarations) and\n<code>MANAGED-CODEX.md</code>, and the manifest pins every registry dependency to an exact\nversion. The package has no cross-package source imports, so a consumer\ninstalls it with only its declared dependencies. Runtime facilities sit behind\nsmall ports — <code>loopback-server.ts</code> uses <code>node:http</code> and <code>sqlite-port.ts</code> lazily\nopens <code>bun:sqlite</code> or <code>node:sqlite</code> — so the built entry runs under both Bun\n≥1.3.14 and Node ≥22.13 (the first release where <code>node:sqlite</code> loads\nunflagged). Under Node, the first account-database open may emit Node's\n<code>ExperimentalWarning</code> for <code>node:sqlite</code> on stderr; the API and file semantics\nare pinned to match <code>bun:sqlite</code>. The managed native Codex launch path still\nrequires its pinned Bun 1.3.14 runtime and fails closed anywhere else — runtime\npinning is an admission invariant, not a portability gap. The repository gate\n<code>bun run check:package</code> builds and\npacks the tarball, scans its contents, verifies the manifest contract and\ndependency completeness, installs it into an isolated consumer, and executes\nthe public entry — including an account-lease custody round trip — under both\nruntimes. Releases are published through the repository's\n<code>v&lt;version&gt;</code> tag channel: an immutable GitHub Release tarball is the\ncanonical artifact and <code>@hraness/xcb</code> on npm is an exact-byte mirror\npublished with OIDC provenance. See <code>docs/publishing.md</code> for the release\ncontract.</p>\n<h2 id=\"command-line-interface\">Command-line interface</h2>\n<p>The package ships an <code>xcb</code> executable — a standalone terminal interface\nthat drives the same task runtime the library exposes. Installing the package\nputs <code>xcb</code> on the PATH:</p>\n<pre><code class=\"language-sh\">npm install -g --ignore-scripts @hraness/xcb\nxcb doctor            # inspect provider binaries, admit this runtime\nxcb auth claude       # sign in with a Claude subscription\nxcb auth status       # show stored sign-in state\nxcb auth logout       # remove the stored credential\nxcb                   # open the chat in the current directory\nxcb run -p &quot;task&quot;     # one headless turn (--cwd picks the workspace)\nxcb sessions          # list local sessions\nxcb sessions rm &lt;id&gt;  # remove a session and its transcript\nxcb sessions prune    # drop sessions idle over 30 days (or N days)\nxcb resume [id]       # continue a session (default: most recent)\n</code></pre>\n<p>Assistant text streams into the chat as the provider completes each content\nblock, and provider-declared errors (for example a plan's session limit) print\ntheir own message next to the typed outcome code. Piped output stays clean:\nstreaming, spinners and ANSI styling only engage on a TTY.</p>\n<p><code>xcb</code> is the kernel layer: one local CLI that keeps provider account\ncustody, process lifecycle, brokered workspace tools, and unified responses on\nthis machine. Cloud sync and orchestration belong to higher-level products\nbuilt on this package; sessions are local-only.</p>\n<p>The chat keeps the model's entire tool surface inside the opened directory:\n<code>workspace.list</code>, <code>workspace.read</code>, <code>workspace.search</code>, <code>workspace.write</code>, and\nbounded public <code>web.fetch</code>. There is no shell, process, or arbitrary-path\noperation. Writes are atomic and require the file's current revision, so a\nstale or speculative edit fails instead of clobbering. <code>/help</code> lists the\nin-session commands; Ctrl-C cancels a running turn and Ctrl-D exits.</p>\n<p>State lives under <code>~/.xcb</code> (mode <code>0700</code>, override with\n<code>XCB_STATE</code>): a SQLite session registry, bounded JSONL transcripts,\nper-provider config directories, the local admission records <code>doctor</code> writes,\nand the subscription credential <code>auth</code> stores.</p>\n<p><code>xcb auth claude</code> runs <code>claude setup-token</code> to mint a long-lived\n(one-year) subscription OAuth token, captured and stored mode-0600 in the\nprivate state root — not the shared login keychain, so it cannot overwrite or\nbe overwritten by a normal <code>claude</code> sign-in. The token reaches the provider\nonly as <code>CLAUDE_CODE_OAUTH_TOKEN</code> inside the run's environment; it is never\nwritten into a workspace or the managed config directory. Claude's config\ndirectory is still redirected so provider hooks, plugins, skills and settings\ncannot leak into a task.</p>\n<p>On macOS each Claude run executes under a seatbelt profile: the provider\nprocess can exec only its own verified snapshot, write only to the per-run\nscratch and the managed config directory, and reach the network only over TCP\n443 and the system resolver — with no keychain, Mach credential service, or\nother-binary execution access (a provider's attempts to spawn <code>sh</code>, <code>git</code> or\n<code>security</code> are denied and observed). On Linux each run plans through an\nadmitted <code>bwrap</code> artifact: private user/mount/pid/net namespaces, read-only\nbinds for the snapshot and its library closure, and egress through the\nsession's unix-socket bridge via the shipped in-namespace CONNECT forwarder\n(<code>sandbox/loopback-forwarder.cjs</code>), which hands the provider standard\n<code>HTTPS_PROXY</code> semantics on a loopback port — no provider cooperation needed.\nWhen that surface cannot be admitted (no bwrap, or a host that refuses\nunprivileged user namespaces — stock Ubuntu 23.10+ requires\n<code>sysctl kernel.apparmor_restrict_unprivileged_userns=0</code>), the CLI refuses to\nrun rather than fall back unsandboxed. Other platforms keep bounded-process\ncustody without an OS-confinement claim. The sandbox is enforcement on top of\nthe broker boundary, not a substitute for it.</p>\n<p><code>doctor</code> inspects the provider binary (explicit <code>XCB_CLAUDE</code> /\n<code>XCB_CODEX</code> pin, then PATH and known install locations), requires the\nexact pinned version, records its SHA-256, and writes a time-boxed local\nadmission record binding that executable, this runtime build, and the\ncapability profile digest. Any drift — a replaced binary, a new release, an\nedited profile — revokes admission until <code>doctor</code> runs again. The adapter\nre-proves the effective boundary on every run: <code>doctor</code>'s record is a gate,\nnot a sandbox attestation.</p>\n<p>Claude is the working provider today. Codex discovery is implemented, but\nmanaged sign-in and task admission stay gated: they require the trusted\nprotocol manifest and pinned parent runtime described in\n<a href=\"https://github.com/hraness/xcb/blob/main/MANAGED-CODEX.md\">MANAGED-CODEX.md</a>, which a local install cannot\nself-produce. <code>--provider codex</code> fails closed until that evidence exists.</p>\n<h2 id=\"migrating-from-agentmixer\">Migrating from AgentMixer</h2>\n<p>Release 0.4.0 renamed the compatibility package's public identifiers from\nAgentMixer to xcb: <code>@hraness/agentmixer</code> → <code>@hraness/xcb</code>, the <code>agentmixer</code>\nexecutable → <code>xcb</code>, <code>~/.agentmixer</code> → <code>~/.xcb</code>, <code>AGENTMIXER_*</code> environment\nvariables → <code>XCB_*</code>, <code>agentmixer.*</code> schema ids → <code>xcb.*</code>, <code>agentmixer_*</code>\nSQLite tables → <code>xcb_*</code>, and the <code>AgentMixer</code> runtime class → <code>Xcb</code>.</p>\n<p>Existing state is never renamed or overwritten silently:</p>\n<ul>\n<li>Run <code>xcb migrate</code> once to copy <code>~/.agentmixer</code> (or <code>$AGENTMIXER_STATE</code>) into\nthe canonical root. The target must be empty; the legacy directory is left\nuntouched so an older install still works — remove it yourself when ready.</li>\n<li>Alternatively, point <code>XCB_STATE</code> at the existing directory; the\n<code>agentmixer_*</code> SQLite tables rename to <code>xcb_*</code> lazily on first open either\nway.</li>\n<li><code>AGENTMIXER_CLAUDE</code> / <code>AGENTMIXER_CODEX</code> binary pins are still honored when\nthe <code>XCB_*</code> variable is unset; rename them when convenient.</li>\n<li>Update dependents: package imports use <code>@hraness/xcb</code>, the runtime class is\n<code>Xcb</code>, and shell invocations use <code>xcb</code>. The last AgentMixer release line is\n<code>@hraness/agentmixer@0.3.0</code> under tag <code>v0.3.0</code>.</li>\n</ul>\n<h2 id=\"application-owned-capability-profiles\">Application-owned capability profiles</h2>\n<p>An application can define its own tools with <code>createCapabilityProfile()</code> and\nbind them to one host-selected workspace and run with <code>createCapabilityBroker()</code>.\nThe host supplies every descriptor, input parser and handler. Model arguments\ncannot replace the bound workspace, credentials or handler implementation. This\nseparate interface leaves Textbutler's existing contact broker and\n<code>Xcb.run()</code> path unchanged.</p>\n<p>For example, this host stores bounded notes in memory:</p>\n<pre><code class=\"language-ts\">import { createCapabilityProfile, createCapabilityBroker } from &quot;@hraness/xcb&quot;;\n\nconst notes = new Map&lt;string, unknown&gt;();\nconst hostState = { active: true };\nconst profile = createCapabilityProfile({\n  id: &quot;notes&quot;, version: 1,\n  tools: [{\n    name: &quot;notes.write&quot;, description: &quot;Replace the bound workspace's note.&quot;,\n    inputSchema: {\n      type: &quot;object&quot;, properties: { text: { type: &quot;string&quot;, minLength: 1, maxLength: 4096 } },\n      required: [&quot;text&quot;], additionalProperties: false,\n    },\n    parseInput(input) {\n      if (typeof input.text !== &quot;string&quot; || !input.text.length\n        || Buffer.byteLength(input.text) &gt; 4096) throw new Error(&quot;INVALID_NOTE&quot;);\n      return input;\n    },\n    execute(input, context) {\n      context.assertActive();\n      notes.set(context.workspaceId, input);\n      return { stored: true };\n    },\n  }],\n});\nconst broker = createCapabilityBroker({\n  profile, workspaceId: &quot;workspace-1&quot;, runId: &quot;run-1&quot;,\n  isActive: () =&gt; hostState.active,\n});\nawait broker.invoke(&quot;notes.write&quot;, { text: &quot;First note.&quot; });\nawait broker.close();\n</code></pre>\n<p>Profiles and their descriptors are immutable. Their SHA-256 digest binds the\nprofile ID, version and ordered tool descriptors, including each input schema.\nIt does not identify handler code or prove runtime confinement; the host must\nestablish that provenance and qualify the adapter for the exact profile separately.\nThe broker checks a closed outer input object; trusted parsers enforce the full\nsemantic contract. Schema descriptors do not fetch references or execute code.</p>\n<p>Calls are serialized, and inputs are copied before queuing. JSON inputs and\noutputs are bounded to 256 KiB, with structural limits; schemas are limited to\n32 KiB per tool and profiles to 64 tools. Unknown tools are denied. Revocation or\nan aborted signal prevents queued work and withholds late results. A trusted\nhandler must call <code>context.assertActive()</code> immediately before every effect,\nincluding after its own awaits, and retain the application's conditional-write\nand authorization checks. Revocation cannot undo an effect already performed.\n<code>revoke()</code> stops admission immediately; <code>close()</code> also waits for admitted handlers\nto settle. Neither proves that an external provider process has stopped.</p>\n<p>Use <code>Xcb.runTask(request, broker)</code> with explicitly supplied <code>taskAdapters</code>\nfor application profiles. The request selects the exact route, authentication\nkind, account, profile, model, reasoning effort, service tier and run limits.\nThe adapter needs current qualification for that exact route, runtime and profile.\nNo live task adapter is bundled, and a selected subscription route is never\nreplaced with an API route. Registering a capability profile does not enable a\nprovider or establish account availability.</p>\n<h2 id=\"opt-in-claude-api-route\">Opt-in Claude API route</h2>\n<p><code>createClaudeApiAdapter()</code> is an additional, explicitly selected <strong>Claude API</strong>\nroute. It does not run Claude Code, use a coding-agent subscription, discover\naccounts, or substitute itself for a selected Codex/Claude Code route. The host\nexecutes its tool loop: API responses can invoke only the supplied broker's six\noperations. No shell, subprocess, native configuration, plugin or server-side\ntool is exposed. Unknown tool names receive a denied result; unexpected response\ncapabilities fail the run. Classification sends an empty tool array.</p>\n<p>This adapter admits its code-enforced execution profile after checking the pinned\nAnthropic SDK 0.125.0 and verifying the compiled runtime's exact bytes. The\n24-hour runtime qualification describes that local authority boundary; it does\nnot attest live account availability or messaging delivery. Recreate an expired\nadapter and refresh model availability before continuing. Native fixture receipts\nplay no part in API admission.</p>\n<p>The trusted embedding host supplies <code>runtimeArtifact: {entrypoint, sha256}</code> from\nits reviewed compiled distribution. The adapter verifies the physical regular\nfile and exact bytes, then binds the runtime identity to that digest and pinned SDK\nversion. A colocated self-authored manifest is not proof of authenticity: the host\nmust establish its distribution signature or reviewed build provenance separately.\nThis input must never come from contact settings or an untrusted plugin.</p>\n<p>The host must explicitly bind a credential and supply observed prices:</p>\n<pre><code class=\"language-ts\">const credentials = createFileClaudeApiKeyResolver({\n  directory: privateCredentialDirectory,\n  bindings: { &quot;owner-api-account&quot;: &quot;anthropic-api-key&quot; },\n});\nconst modelCatalog = await discoverClaudeModels({\n  credentials,\n  accountId: &quot;owner-api-account&quot;,\n  priceCatalog: ownerReviewedPrices,\n  signal,\n});\nconst adapter = await createClaudeApiAdapter({\n  runtimeArtifact: verifiedCompiledRuntime,\n  credentials,\n  modelCatalog: async () =&gt; modelCatalog,\n});\n</code></pre>\n<p>The credential directory must be physical, owned by the current user and mode\n0700. Each selected file must be a single-link regular file with private read/write\npermissions; it contains only the API key, optionally followed by one newline.\nFiles are read through bounded, checked descriptors on each use. The application\nowns credential creation/removal and keeps this directory outside contact memory.\nThe existing explicit environment-variable resolver remains available.</p>\n<p>Model discovery calls only the authenticated Models API. It does not send a\nprompt or execute an agent. <code>ClaudePriceCatalog</code> contains <code>observedAt</code> and exact\n<code>id</code>, <code>inputUsdPerMillion</code>, <code>outputUsdPerMillion</code>, <code>classifierEligible</code> entries;\nprices older than 30 days are rejected. The API supplies availability and structured\noutput capability, not prices. Unknown/unpriced models remain excluded. The\nclassifier selects the lowest estimated cost from the eligible observed entries.</p>\n<p>Requests go only to the fixed Anthropic HTTPS origin, with explicitly reconstructed\nheaders, no cookies, redirects, retries, custom endpoints or ambient proxy\nconfiguration. Responses are bounded before SDK parsing. Defaults are eight\nturns, 4,096 output tokens (512 for classification), a 120-second deadline and\na $0.25 conservative local reservation using supplied prices. That reservation\nis not a provider billing cap. Cancellation prevents subsequent tool calls and\nreleases account custody once all awaited local work has stopped. Errors omit\nprovider response bodies and credentials.</p>\n<h2 id=\"gobstopper-preset-command-shim\">Gobstopper preset command shim</h2>\n<p><code>src/gobstopper-editor.ts</code> is a source-checkout executable that backs\ngobstopper's <code>agentic</code> compaction strategy through <code>runAgentTask()</code>. It reads\nthe normalized transcript JSON a <code>preset.command</code> receives on stdin, presents a\nfixed <code>keep</code>/<code>elide</code>/<code>summarize</code>/<code>defer</code> capability profile to the cheapest\n<code>selectClassifierModel()</code>-eligible Claude model through a bounded Messages-API\ntask adapter, and writes <code>{&quot;edits&quot;: [...], &quot;context_tokens_after&quot;: n}</code> on\nstdout — the exact <code>Edit</code> wire shape gobstopper parses. Diagnostics go to\nstderr; a nonzero exit marks failure. The shim lives in <code>src/</code> only: it is a\nhost integration tool, not part of the packed <code>dist</code> contract.</p>\n<pre><code class=\"language-toml\">[presets.xcb]\nstrategy = &quot;agentic&quot;\ncommand = &quot;bun /path/to/xcb/src/gobstopper-editor.ts&quot;\n</code></pre>\n<p>The editor tools only record the model's calls through the capability broker;\nthere is no filesystem, shell or network tool surface. Recorded calls lower to\n<code>Edit</code> objects with <code>calls_to_plan</code> semantics: elide positions map through\n<code>items[i].line_index</code> for elidable items only, summarize injects a digest,\nkeep is advisory, and any defer emits an empty edit list. A protected tail of\nthe last eight items (<code>GOBSTOPPER_EDITOR_PROTECT_TAIL</code>) is never touched.\nAccount custody uses a per-process SQLite lease store; no cross-process\naccount coordination is claimed.</p>\n<p>Authentication needs a bound Anthropic API key — <code>ANTHROPIC_API_KEY</code> by\ndefault, another variable via <code>GOBSTOPPER_EDITOR_KEY_ENV</code>, or an owner file via\n<code>GOBSTOPPER_EDITOR_KEY_DIRECTORY</code>. Model choice comes from a fresh\nhost-observed catalog: <code>GOBSTOPPER_EDITOR_MODEL_CATALOG</code> points at a\nModelCatalog JSON, otherwise live Models discovery runs against the built-in\nlist prices (overridable via <code>GOBSTOPPER_EDITOR_PRICES</code>). <code>--dry-run</code>\nvalidates stdin and prints an empty plan without provider work.</p>\n<h2 id=\"native-coding-agent-execution-status\">Native coding-agent execution status</h2>\n<p>Managed Codex account controls are separate from agent execution.\n<code>createManagedCodexAccountController()</code> provides subscription sign-in, cancellation,\nsign-out, account checks and bounded model discovery through a host-supplied\naccount-only transport. <code>createCodexAccountStdioTransport()</code> implements the\nsupported app-server account protocol over an explicitly supplied process port.\nNeither function launches a production process or qualifies a response adapter.\nSee <a href=\"https://github.com/hraness/xcb/blob/main/MANAGED-CODEX.md\">managed Codex account integration</a> for the host contract.</p>\n<p><code>createClaudeSdkAdapter()</code> implements the pinned Claude Agent SDK subprocess\nprotocol with API-key authentication. Its execution gate requires a trusted host\nqualification for the exact native executable and SDK digest. No production\nqualification receipt is bundled. The default provider adapters continue to refuse\nexecution. <code>createProviderLaunchPlan()</code> is descriptive configuration, not a sandbox.</p>\n<p>The installed versions are Claude Agent SDK <strong>0.3.268</strong>, bundled native Claude Code\n<strong>2.1.268</strong>, Anthropic SDK <strong>0.125.0</strong>, MCP SDK <strong>1.30.0</strong>, and Zod <strong>4.6.2</strong>.\n<code>inspectClaudeSdkRuntime()</code> checks the installed SDK version, native binary owner,\nmode, link count and SHA-256, and returns the composite qualification identity.\nEvery run verifies and copies executable bytes from a checked file descriptor into\nits private run directory before resolving credentials. The subprocess runs that\nsnapshot, so replacing the configured source path cannot replace the admitted\ncredential-bearing executable.</p>\n<p>The adapter creates separate private working, home, configuration and temporary\ndirectories outside contact memory. It supplies an explicit environment, removes\nall built-in tools, disables inherited settings, hooks, automatic memory, connectors,\nplugins, bundled skills, workflow triggers and persistence, and configures only its in-process MCP broker. It checks the\nnative initialization model, version, tools, MCP servers, skills, plugins and API-key\nsource before admitting broker calls. A fixed plain-text prompt header prevents task text from entering native slash or bang command dispatch. Classification has no tools. The contact\nfolder is accessed only by host broker methods; it is never the native process cwd.</p>\n<p>Native processes use a detached process group. Raw stdout is bounded to 1 MiB per\nframe and 8 MiB total before SDK parsing; stderr is discarded and capped at 256 KiB.\nCancellation terminates the group, escalates to kill if needed, and waits for root\nexit and group absence before releasing account custody. A joined failure uses\n<code>AgentStoppedError</code>; an unproven exit keeps its lease and private state. These\ncontrols do not prove confinement of a malicious native process or a descendant\nthat escapes its process group. Runtime qualification must cover the trusted\nexecutable, host policy and relevant descendant behavior independently.</p>\n<p>The host can bind an account to one explicit environment variable:</p>\n<pre><code class=\"language-ts\">const credentials = createEnvironmentClaudeApiKeyResolver({\n  &quot;owner-api-account&quot;: &quot;TEXTBUTLER_ANTHROPIC_API_KEY&quot;,\n});\nconst adapter = createClaudeSdkAdapter({\n  runtime: { executablePath: pinnedNativePath, executableSha256: reviewedBinarySha256 },\n  stateRoot: privateProviderStateDirectory,\n  credentials,\n  qualification: independentlyVerifiedHostQualification,\n});\n</code></pre>\n<p>Those paths, digest and qualification are host inputs, never contact or plugin\nconfiguration. The resolver reads only the selected variable at invocation time;\nit does not discover personal accounts, parse dotenv files, use ambient provider\nkeys, or borrow subscription tokens. It admits the pinned API-key format and\nrejects OAuth tokens. A host Keychain integration can implement the same\n<code>ClaudeApiKeyResolver.withApiKey()</code> interface without changing the broker. API keys\nmust remain outside contact folders, settings responses and logs. The adapter's\nper-run default budget is $0.25 and deadline is 120 seconds; neither is a claim of\naccount-wide spend control.</p>\n<p><code>test/claude-sdk.test.ts</code> runs the real SDK against synthetic subprocess peers to\nverify option isolation, MCP routing, zero-tool classification, output validation,\nrevocation and process custody. <code>test/provider-process.test.ts</code> also proves\ntermination of a surviving process-group descendant and raw output bounds. On\n2026-09-11 the actual macOS ARM64 native CLI completed a separate control\ninitialization with a fresh synthetic home and no user message: the configuration\nwas accepted and the MCP inventory was empty. That control response contains no\nbuilt-in tool inventory and is insufficient for execution qualification.\n<code>qualification/claude-native.ts</code> is the explicit adversarial native fixture with a\nlocal synthetic Anthropic endpoint and temporary contact folders. It uses the same\nrestricted launch-option builder as production; the production adapter does not\naccept custom API endpoints. Its receipt describes the exact fixture/runtime\nboundary and never automatically enables production. The seven native scenarios\npassed on macOS ARM64 on 2026-09-11, including explicit <code>doctor</code> / <code>checkup</code> Skill\ndenials, literal command-shaped prompts, 11 forbidden tool calls, six file escape\nor stale-write denials, and a successful conditional edit and staged reply.</p>\n<p>The <a href=\"https://code.claude.com/docs/en/agent-sdk/skills\">SDK skills reference</a>\ndistinguishes the discovered skill catalog from invocation permission. An empty\nallowlist alone does not empty that catalog, and direct command dispatch bypasses\nthe allowlist. The pinned profile explicitly turns <code>doctor</code> and <code>checkup</code> off,\ndenies the Skill tool, wraps task text, and retains strict empty-catalog checks as\na configuration-drift guard. Native evidence checks every API request's tool\nmanifest and actual denied results; catalog absence alone is not a scope proof.</p>\n<p>Codex remains unavailable in the app. The experimental <code>codex-config.ts</code>,\n<code>codex-process.ts</code>, <code>codex-relay.ts</code> and <code>codex-session.ts</code> modules implement a\npinned <strong>0.153.4</strong> app-server driver. They have no credential discovery, live\nprovider transport or production adapter registration. The launcher copies the\nverified executable into a private runtime directory, keeps contact folders\noutside native scratch, and records process custody before protocol startup.\nThe trusted host must also supply an independently admitted SHA-256 for its\nactual Bun 1.3.14 executable. The launcher checks that identity before startup\nand after joined shutdown. Before spawning, it verifies the private scratch\ndirectory's closed inventory, permissions, link counts and exact configuration\nbytes. Version 2 custody records bind the parent runtime and scratch digests;\nolder records cannot supply those proofs. Runtime drift retains the run's state.\nThe relay checks the exact ordered tool inventory on every model request, using\nthe pinned native schema representation. Codex omits string-length and numeric\nrange hints on the wire; the broker still enforces those limits. Each result\nstays bound to its native call, exact broker output and stable history identity.\nThe relay bounds native client metadata and removes\nit before forwarding, including that field's workspace and installation\nidentifiers. Classification has no tools. Unsupported\nresponse shapes fail closed; the current relay accepts a deliberately small\nResponses SSE contract and is not a general provider streaming implementation.\nRemote control is explicitly disabled at CLI startup. Its disabled-state\nnotification and bounded native timestamps carry no authority; active states and\nunreviewed notifications fail the session.</p>\n<p>The internal <code>codex-api-response.ts</code> module admits a narrow, buffered OpenAI\nResponses JSON result and translates it into that three-event contract. It binds\none dated model snapshot, exact broker descriptors and an output-token cap,\npreserves measured usage, and rejects incomplete responses, reasoning, duplicate\nJSON keys and extra effects. It makes no HTTP request and is not an API adapter.\nThe host still needs credential and account custody, request construction,\nbilling admission, response-body cleanup and cross-response call-ID checks. No\nCodex API account or model route is registered by importing it.</p>\n<p>On 2026-09-12 the actual pinned native executable completed a scripted response\nthrough all six broker tools and a separate zero-tool classification through\nthese modules. Every request carried the expected inventory; contact memory,\nstaged actions, process exit and listener cleanup matched the fixture. The model,\nweb responses and contacts were synthetic; nothing was sent or billed. These\nchecks establish runtime compatibility, not production qualification. Direct\nfilesystem and process confinement, adversarial runtime custody, and\naccount/model transport admission remain required. Session receipts always\nreport <code>productionQualified: false</code>.\nNine scripted native rejection cases also passed: foreign paths and file aliases,\nstale writes, forbidden shell/skill/input tools, duplicate calls, cancellation,\nan upstream deadline and malformed SSE. They verified unchanged protected\nfixtures, no staged sends, and joined process, broker and listener cleanup.\nAn adversarial kernel helper also demonstrated that host-supplied ordinary file\ndescriptors and preexisting hard links retain access across sandbox startup.\nProduction admission must prove the launcher's clean descriptor and scratch\nsetup; the profile alone cannot undo authority supplied by the host.\nA separate helper test under pinned Bun 1.3.14 passed on 2026-09-12: the child\nhad exactly three communication sockets, all four deliberately inheritable\nparent descriptors were absent, and process and listener cleanup joined. That\ntest substituted a descriptor-inspection helper for Codex and does not qualify\nthe native agent itself.\nSee the <a href=\"https://learn.chatgpt.com/docs/config-file/config-reference\">Codex configuration reference</a>\nand <a href=\"https://learn.chatgpt.com/docs/app-server\">App Server documentation</a>.</p>\n<p>The <a href=\"https://code.claude.com/docs/en/agent-sdk/custom-tools\">Claude custom-tools documentation</a>\ndocuments selecting built-ins with <code>tools</code>; <code>tools: []</code> removes that surface.\nThe <a href=\"https://code.claude.com/docs/en/agent-sdk/permissions\">permissions documentation</a>\nexplains why <code>allowedTools</code> alone only pre-approves calls. A production qualification\nmust also establish that managed host policy has not introduced configuration,\nhooks or other authority that cannot be disabled by application settings.</p>\n<p><code>createDevinAcpAdapter()</code> implements the Agent Client Protocol against\n<code>devin acp</code>, Devin CLI's stdio JSON-RPC server (protocol version 1, observed on\nCLI 3000.10.x). Each run spawns one bounded child through the application's\n<code>BoundedProviderProcessFactory</code>, initializes with <code>fs</code> and <code>terminal</code> client\ncapabilities unimplemented, opens one session with the host-selected workspace\ncwd, applies the pinned mode and model through <code>session/set_mode</code> and\n<code>session/set_config_option</code>, and completes a single <code>session/prompt</code>. The\nframing codec bounds every line to 1 MiB and validates JSON-RPC envelopes,\nidentifiers and control characters before dispatch; prompts are bounded to\n256 KiB; pending requests are capped and every write is serialized.</p>\n<p>Devin advertises <code>mcpCapabilities: {http: false, sse: false}</code> — only stdio MCP\ntransports exist. Profile tools therefore reach the agent through\n<code>startDevinToolRelay()</code>, a host-owned loopback endpoint admitted by a random\npath and bearer token, plus a spawned inline bridge process\n(<code>DEVIN_MCP_BRIDGE_SOURCE</code>) that answers <code>tools/list</code> and <code>tools/call</code> over\nstdio. The bridge is a transport adapter only: the broker still enforces the\nexact profile manifest, input bounds, workspace scoping and revocation.\nInbound <code>session/request_permission</code> calls are answered by the host's\npermission callback; without one, the first reject option wins. <code>session/delete</code>\nis never invoked by this adapter; provider sessions may persist for host\n<code>session/load</code> recovery.</p>\n<p>Custody mirrors the other task adapters: <code>stopAndJoin()</code> proves process-group\nexit before account release, an unproven join fails close and retains the\nlease, and protocol cleanup is never treated as termination evidence. Usage is\nreduced from <code>usage_update</code> facts and the prompt result into the neutral\n<code>AgentTaskUsage</code> shape; no billing or quota claim is made. <code>AgentProvider</code>\naccepts <code>&quot;devin&quot;</code> and <code>createProviderLaunchPlan()</code> emits the Devin\nconfiguration, but the provider remains unqualified: exact-runtime adversarial\nqualification, effective tool inventory, stdio bridge custody, failure custody\nand account transport/model admission are all unresolved. <code>test/devin-acp.test.ts</code>\nand <code>test/devin-adapter.test.ts</code> exercise the codec, client, relay and spawned\nbridge against synthetic peers only; they establish no live provider evidence.</p>\n<h2 id=\"per-account-browser-sessions\">Per-account browser sessions</h2>\n<p><code>createBrowserSession()</code> is the provider-neutral custody substrate for\nbrowser-based sign-in. Each provider account gets one dedicated, persistent\nChromium-family profile under\n<code>&lt;stateRoot&gt;/browser-sessions/&lt;provider&gt;/&lt;accountId&gt;/profile/</code> — cookies and\nsite state survive restarts inside that directory only, so adding or rotating\nan account never touches another account's session. The profile directory is\nthe custody boundary: cookie contents are never exported, logged, synced, or\nincluded in receipts.</p>\n<p>A session binds to the exact <code>{provider, accountId, owner, leaseGeneration, processGeneration}</code> and holds an exclusive <code>O_EXCL</code> lock for the life of the\nprocess. A lock that outlives its process is never taken over silently — the\nnext launch refuses with <code>BROWSER_SESSION_RECOVERY_REQUIRED</code>, and\n<code>recoverBrowserSession()</code> proceeds only after a caller-supplied\n<code>proveStopped(binding)</code> returns true. Recovery removes the Chromium\n<code>SingletonLock</code>/<code>SingletonSocket</code>/<code>SingletonCookie</code> trio so the profile opens\ncleanly; it never deletes cookies, history, or the binding marker.\n<code>purgeBrowserSession()</code> is the sign-out/revocation boundary: it destroys the\naccount's whole browser-session directory as a unit, and likewise requires\nstop proof while any lock is held.</p>\n<p>Launch admits only a caller-pinned executable whose SHA-256 is re-verified\nfrom a checked descriptor before spawn. The argv is fixed: the account's\n<code>--user-data-dir</code>, <code>--password-store=basic</code> (profile-local secrets, no keyring\nprompts), <code>--disable-sync</code> (no vendor account bleed), and at most one\nhttps-only navigation URL. The environment is an allowlist — locale,\nidentity, and GUI-attach keys only — with <code>HOME</code>/<code>TMPDIR</code> overridden to\nprivate per-run scratch. The browser runs in its own detached process group\nwith bounded stdout/stderr; close escalates SIGTERM (so the profile flushes)\nthen SIGKILL, and the lock is released only after root exit, group absence,\nstream joins, and a durable hash-chained custody journal record. An unproven\ncleanup keeps the lock and the journal as recovery evidence.</p>\n<p>Every receipt reports <code>productionQualified: false</code>: this module proves custody\nand launch integrity, not browser provenance, sandbox qualification, or live\nprovider authentication. Those remain separate host responsibilities.</p>\n<h2 id=\"managed-account-custody\">Managed account custody</h2>\n<p><code>createManagedAccountController()</code> is the provider-neutral account-lifecycle\nport every managed provider controller shares. It owns the custody discipline\n— the shared SQLite account lease, the exact <code>{provider, accountId, owner, leaseGeneration, processGeneration}</code> binding, a serialized\nunchecked → signing-in → signed-in state machine, unresolved-login recovery,\nand the joined-close barrier that must prove process exit, group absence,\nstream joins and settled requests before the lease is released. A transport\nthat loses a <code>startLogin</code> response is <code>recovery-required</code>, never a retryable\npending login, and only the proven close retires the possibly-live process.</p>\n<p>Providers supply two ports and nothing else: a <code>ManagedAccountTransport</code>\n(closed <code>accountRead</code>/<code>startLogin</code>/<code>cancelLogin</code>/<code>logout</code>/<code>close</code> — no raw\nRPC, token export or turn method) and <code>ManagedAccountSemantics</code>, which\nprojects the provider's account payload onto the neutral readiness state.\nLogin methods are the closed union <code>provider-native</code> (the provider's own\nflow) and <code>browser-session</code>, which binds sign-in to the caller's per-account\nbrowser custody session — the session's provider, accountId and owner must\nequal the account's, so one account's cookie jar can never authenticate\nanother. <code>readUsage()</code> surfaces a bounded, read-only quota observation\nthrough an optional admitted <code>ManagedAccountUsageReader</code>: provider-named\nwindows with optional utilization, an exact-response SHA-256, and no\ncredentials or raw payloads. A missing reader returns <code>null</code> — it is not a\nzero-usage claim. Nothing here is execution or authentication\nqualification; provider runtime admission remains separate host evidence.</p>\n<h2 id=\"os-confinement-port\">OS-confinement port</h2>\n<p><code>src/os-sandbox.ts</code> is the provider-neutral boundary between a closed launch\nspecification and the platform sandbox mechanism. A backend turns an\n<code>OsSandboxSpec</code> — the admitted executable snapshot, the per-run scratch root,\nan optional persistent account root, a closed read-only file list, a network\npolicy label and the durable policy-artifact path — into an <code>OsSandboxPlan</code>.\nPlanning is asynchronous so a backend can re-verify its own wrapper artifact\nfrom a checked descriptor; the plan's <code>wrap()</code> is synchronous so it can run\ninside provider SDKs that spawn from a synchronous callback. The spec rejects\nrelative paths, control bytes, undeclared fields, writable roots that contain\nor enclose the executable, overlapping writable roots, and a policy artifact\nplaced inside a writable root.</p>\n<p>Two backends ship with the port. <code>createSeatbeltOsSandbox()</code> (macOS) accepts a\nhost-owned reviewed SBPL generator and wraps argv as the fixed literal\n<code>/usr/bin/sandbox-exec -f &lt;policy&gt; &lt;executable&gt; ...</code> — the managed Codex\nlaunchers now plan through it, byte-identically to their previous inline\nbehavior. <code>createBwrapOsSandbox()</code> (Linux) re-verifies the admitted <code>bwrap</code>\nbinary's SHA-256 at plan time and emits private user/mount/pid/ipc/uts/cgroup/\nnet namespaces, per-file <code>--ro-bind</code> entries, <code>--bind</code> for the writable roots,\n<code>--die-with-parent</code>, <code>--new-session</code>, <code>--clearenv</code>, and the closed environment\nrebuilt in sorted <code>--setenv</code> order. Bubblewrap cannot express per-destination\negress, so on Linux <code>network: &quot;provider-tcp443-dns&quot;</code> is plannable only with an\nadmitted <code>egressSocket</code>: a host-side unix-socket CONNECT bridge bound into the\nnamespace as its own read-write mount. The in-sandbox runtime never performs\nDNS or TCP itself — it speaks <code>CONNECT host:443</code> over the socket and the host\nbridge resolves and dials, refusing every port but 443 and any host outside\nthe admitted exact-host allowlist. The network namespace stays unshared either\nway, so the socket is the child's only egress path; without one, provider\nnetworking still refuses to plan.</p>\n<p>Two consumption paths exist. A cooperative in-sandbox runtime uses the public\nconsumer (<code>src/egress-client.ts</code>): <code>connectEgress</code> /\n<code>connectEgressTls</code> / <code>createEgressHttpsAgent</code> / <code>fetchViaEgress</code> speak the\n<code>XCB_EGRESS_SOCKET</code> contract directly, bound CONNECT size and response\nbytes, pin TLS SNI to the target host, and refuse anything but HTTPS on 443.\nA stock binary that does not know the contract gets an <code>egressForward</code> spec\nentry instead: an admitted JS runtime and the shipped forwarder script become\nthe namespace entry point, the forwarder serves <code>CONNECT</code> on a fixed loopback\nport, and the child receives <code>HTTPS_PROXY</code> — the private netns is created\nempty, so the fixed port cannot collide. The bridge itself remains an\ninternal host seam (<code>src/egress-bridge.ts</code>), not a public export. There\nis no fallback — a spec whose admitted platform the backend cannot enforce,\nan unverified artifact, or an unexpressible policy refuses the plan rather\nthan launching unsandboxed. The spec's <code>platform</code> is admission evidence about\nthe runtime being launched, not the build host: a backend refuses a spec whose\ndeclared platform it cannot enforce, so synthetic custody tests exercise the\nreal launch path on any host.</p>\n<p><code>createSandboxedProviderProcessFactory(plan)</code> composes a plan onto the\nunchanged bounded-provider custody — stdout/stderr bounds, the detached\nprocess group and SIGTERM/SIGKILL join are identical underneath every backend\n— and <code>verifyOsSandboxExecutable()</code> re-checks an admitted artifact's owner,\nfile identity, no-follow canonical path and SHA-256. A plan proves policy\nconstruction and artifact admission only, never kernel enforcement; receipts\ncontinue to report <code>productionQualified: false</code>. <code>qualification/linux-sandbox.ts</code>\nis the explicit kernel-boundary fixture for the bwrap backend: a statically\nlinked synthetic canary asserts scratch writes, foreign-path absence, a\nrouteless network namespace and PID-namespace isolation on the host that runs\nit, and reports blocked evidence instead of guessing when the toolchain or\nnamespaces are unavailable. <code>qualification/linux-egress.ts</code> is the bridge\nboundary's companion: the canary asserts the mounted socket answers CONNECT,\nbytes tunnel through it, a foreign unix path and a direct TCP connect are\ndenied inside the same namespace, and the join receipt reports listener,\nsocket-set and path removal — against a synthetic dialer, with no resolver\nor provider endpoint involved. <code>qualification/linux-loopback.ts</code> completes\nthe chain: a stock <code>curl</code> under <code>HTTPS_PROXY</code> traverses forwarder → bridge →\na local <code>openssl s_server</code>, proving the stock-binary path without provider\ncooperation. The <code>Qualification</code> workflow runs all three on <code>ubuntu-24.04</code>\nCI and uploads the JSON evidence — including the recorded fact that Ubuntu's\ndefault AppArmor user-namespace restriction blocks bwrap entirely until the\nhost lifts it (<code>kernel.apparmor_restrict_unprivileged_userns=0</code>).</p>\n<h2 id=\"ownership-boundaries\">Ownership boundaries</h2>\n<p>The account and task consumers accept a structural <code>ProviderProcessPort</code> through\n<code>bindCodexAccountProcess()</code> and <code>bindCodexTaskProcess()</code>. Codex sessions serialize\nexplicit write receipts: only <code>accepted-full</code> advances the protocol. Refused,\npartial, unknown or timed-out writes fail the operation without replay; cleanup\nretains any outstanding write and authority work. Task finalization requires the\nmatching physical join plus settled transport and delivery. The host's finalizer\nstill owns its configuration, confinement, scratch and durable custody receipt.\nPhysical join alone does not prove those product facts or qualify a provider.</p>\n<p>A trusted <code>CodexProcessLauncher</code> or <code>CodexManagedProcessLauncher</code> can return that\ntask bridge after preparing its exact account, task, profile and launch intent.\nThe Claude SDK adapter also accepts an optional synchronous <code>processFactory</code>\nbehind its existing runtime, credential and tool checks; omission preserves the\ncurrent bounded process owner. Factories must return an owned handle even when\nreadiness later fails, and may not discard a process after a launch effect. The\nfactory owns artifact admission, native event persistence and complete stop/join\nsemantics. These are source integration seams: no shared native artifact, native\nmanaged launcher or new production qualification is bundled or implicitly enabled.</p>\n<p>The application owns its daemon, contact enrollment, message classification policy,\nconversation history, memory format, prefix formatting and Ghostget/Linq access.\nXcb owns the execution seam. The model cannot choose a workspace or contact\nin broker input. <code>WorkspaceFiles</code> and <code>PublicWeb</code> are trusted host ports. Textbutler supplies its confined file implementation and uses <code>createPublicWeb()</code> by default. Custom replacements must preserve file confinement and public-network policy across DNS and every redirect. URL syntax validation alone is insufficient. The supplied web client admits public unicast addresses, rejects mixed public/private DNS answers, pins the selected address while preserving TLS hostname verification, and validates each redirect anew. It fetches bounded UTF-8 text only; it does not carry account cookies or authorization headers.</p>\n<p>Messaging ports only stage proposed actions and return an intent ID. They must never\nsubmit a message during composition. The application must recheck current enrollment, exact recipient/message ownership,\ncapability support, idempotency and authorization at its final dispatch boundary.\nIt must add the configured butler envelope itself. Arbitrary rich payloads,\nstickers and mini apps are not admitted until a transport contract proves support.</p>\n<p>The web client refuses ambient proxy environment variables because Bun can route\nHTTPS through them despite a disabled connection pool. Its 16 KiB header limit\napplies to accepted headers; Bun buffers headers before that check. The local TLS\nfixture proves address pinning and certificate/hostname enforcement under Bun\n1.3.14. A bounded live GET to <code>https://example.com/</code> also passed on 2026-09-11\n(559 bytes with the expected page title). Tests generate their own temporary\ncertificate with OpenSSL and remove it afterward.</p>\n<p>Accounts are opaque host bindings. Keep provider authentication and native runtime\nstate out of contact folders. Do not copy credentials into a second app or let a\ncontact edit provider configuration. One shared lease database can coordinate\napplications only when all of them use its custody contract; this package does\nnot alter an existing application's running sessions or account authority.</p>\n<p>Lease expiry indicates a missed heartbeat and never authorizes takeover. A failed\nor ambiguous adapter call retains its lease. Recovery needs independent proof that\nthe old process/controller stopped, followed by an exact generation-conditional\nrelease. <code>AbortSignal</code> alone does not prove process exit. A successful adapter result\nmust assert <code>processStopped: true</code> only after obtaining that evidence.</p>\n<p>The current AI Charts CLI collects usage and does not execute agents, so its likely\nfuture shared interface is sanitized usage/account metadata, not this execution\nport. Account sign-in and product-provider terms need separate qualification;\n<a href=\"https://code.claude.com/docs/en/agent-sdk/overview\">Anthropic's SDK overview</a> directs\nthird-party product integrations to supported API authentication unless approved.</p>\n<p><code>createCodexTaskAdapter()</code> is the relay-backed task adapter for application\ncapability profiles. It requires a host <code>CodexResponsesUpstream</code>; selecting a\nsubscription route does not provide subscription authentication. It maps the\nexact <code>CapabilityBroker</code> inventory into one Codex session, passes only host-supplied instructions and task settings, and\nretains the process receipt until <code>Xcb.runTask()</code> has joined the adapter\nstop and broker close. Constructing the adapter does not discover credentials,\nselect an account, or qualify the installed native runtime; those remain explicit\nhost and qualification inputs.</p>\n<p>Each admitted execution owns its stop receipt. A busy adapter returns a failed\ncompletion for the new request without stopping the active account. Stop requests\nmust match the original execution; the same or a shorter cleanup deadline is accepted.\nFailures before session startup carry explicit no-session evidence. Once startup\nbegins, an uncertain launch or missing process-stop receipt retains account custody.</p>\n<p>The task runtime acquires one account lease and includes its immutable\n<code>accountLease</code> snapshot in the execution request. Completion and stop evidence\nmust match its provider, account, owner, generation and expiry. Managed adapters\nand sessions also require runtime provenance through <code>assertAgentTaskAccountLease()</code>:\ncopying a request must preserve the original lease object and <code>signal</code>, with all\nother request values unchanged. Only stop may narrow the cleanup deadline.\nReconstructing the lease from its values does not grant admission. The runtime\nretires this authority when the run settles, even if uncertain cleanup retains\nthe account lease. A managed launcher receives that same lease and must preserve\nthe separate native account-lock and process-generation checks.</p>\n<p>Task run and cleanup allowances share the task runtime's one-hour ceiling;\nthe legacy contact session keeps its 120-second run and 10-second cleanup limits.\nIO, request-count and byte limits remain bounded separately. Unsupported task\nbudgets fail before session startup. Cleanup is still joined even when late;\n<code>runTask()</code> reports a missed cleanup deadline instead of claiming timely closure.</p>\n<p>Non-null reasoning effort and service tier are sent as explicit turn overrides.\nThe relay requires those exact selections in the model request, including when\nreasoning is absent. Null settings leave the native defaults in place. Generic\ntask effort names follow the pinned protocol's bounded string contract, including\n<code>ultra</code>; the older six-tool driver keeps its existing effort inventory.</p>\n<p><code>createCodexManagedTaskAdapter()</code> adds an experimental task transport for the\nofficial Codex app-server's managed ChatGPT authentication. The host must supply\na <code>CodexManagedProcessLauncher</code> that owns the native process and keeps its\nauthentication state outside application workspaces. Codex owns sign-in, token\nrefresh and provider traffic. This adapter has no API upstream or token input.\nThe launcher receives the original\nruntime request, including its original signal and account lease, plus a separate\n<code>cancellationSignal</code>. It must revalidate request authority before preparation and\nnative launch; the mirrored IDs and lease grant no separate authority.</p>\n<p><code>createCodexManagedProcessLauncher()</code> in <code>src/codex-managed-process.ts</code> supplies\ninternal process candidates. Its trusted admission maps the adapter runtime\nidentity to native executable, schema and parent-runtime hashes. The existing\n<code>managed-task-offline-candidate-v1</code> profile remains byte-for-byte unchanged and\ncannot perform provider turns. Missing profile selection is refused.</p>\n<p>The separate <code>managed-task-provider-tcp443-dns-candidate-v1</code> selection appends\nonly outbound access to the system resolver socket and TCP port 443, plus\nmetadata access to <code>/var</code> for resolver path traversal. It grants general TCP\n443 access, including local or private destinations; it enforces neither TLS\nnor a hostname allowlist. Native Codex remains responsible for authenticating\nits provider connection. Model-requested public web access still uses the\nseparate bounded host broker. No additional file contents, Mach services,\nlisteners, forks or model tools are admitted by this profile.</p>\n<p>Profile selection is copied before asynchronous work, and custody receipts\nretain its exact tag and generated policy digest. The provider candidate records\n<code>general-tcp443-system-resolver-var-metadata-candidate</code>; both candidates retain\n<code>productionQualified: false</code>. Account device-code admission does not authorize\ntask networking. These candidates remain absent from the public barrel and\ndefault host, and neither matching pins nor successful sign-in creates task\nqualification. Native provider transport is still unproven, including the\nunresolved account login request-send failure. Activation requires separate\nevidence for authenticated turns, effective tool inventory, filesystem isolation\nand cleanup using the exact selected task profile.</p>\n<p>The host must first close and join account controls, then let <code>runAgentTask</code>\nacquire its account lease. The process owner uses that exact lease and the\nexisting account marker, fixed configuration and <code>active.json</code> lock under the\nsame private account state root. It neither creates an account nor acquires a\nsecond lease. Each run gets a verified immutable executable snapshot, empty\nscratch HOME and cwd, closed environment and durable custody journal. Persistent\nCodex state survives cleanup; contact files remain available only through the\nhost broker. Missing or changed configuration is preserved and refused.</p>\n<p>Cancellation and the original execution deadline initiate joined cleanup.\nAn uncertain launch, process group or descriptor close retains custody. Cleanup\nnever signals a numeric process group after its root has been observed exiting.\nThe first cleanup deadline is retained across retries; later observed closure\nmay complete cleanup without granting another native wait budget. Filesystem\nsync and removal can outlast that deadline, so the retained cleanup promise\nmust still join before any lease release. Receipt hash fields remain empty\nuntil the corresponding snapshots are completed. These synthetic\ncustody checks do not prove native tool inventory or auth-home confinement.</p>\n<p>The managed and account process owners admit a parent runtime of\n<code>darwin|linux</code> × <code>arm64|x64</code> and select the sandbox backend from the inspected\nparent platform. Darwin plans through the seatbelt backend exactly as before.\nA Linux parent requires the runtime admission to carry a <code>sandbox</code> artifact —\nthe pinned <code>bwrap</code> executable SHA-256 plus the host-admitted read-only library\nclosure the copied runtime needs inside the namespace — and plans through the\nbwrap backend with <code>network: &quot;denied&quot;</code>. A Linux parent without that artifact,\nor a Darwin parent carrying one, is refused as a sandbox-admission mismatch.\nA Linux provider-egress profile additionally requires a <code>sandbox.egress</code>\nadmission and a trusted-host <code>startEgressBridge</code> seam: the owner starts the\nunix-socket CONNECT bridge inside the run directory, binds the socket into\nthe bwrap plan, hands the child its path as <code>XCB_EGRESS_SOCKET</code>, and\njoins the bridge — listener closed, sockets joined, socket removed — before\nthe account lock may release. A missing admission, a missing seam, a failed\nstart, or an unproven join refuses or holds custody exactly like any other\nlaunch boundary failure. Neither mechanism is qualified for production:\nreceipts continue to report <code>productionQualified: false</code>. The\n<code>codex-process.ts</code> loopback relay stays Darwin-only\nbecause a network-namespace cut would sever the relay socket it exists to\nserve.</p>\n<p><code>runCodexManagedOfflineDiagnostic()</code> in the same internal module exercises the\nshared process owner without fabricating task qualification. Its separate\n<code>managed-offline-lifecycle-diagnostic-v1</code> admission binds declared native, schema\nand parent-runtime hashes. Each call creates a fresh synthetic account in a\nprivate child directory and acquires real SQLite leases. The offline account\nhelper initializes that empty home, sends no RPC, and joins before releasing its\nlease. A second lease then owns the same managed process core used by tasks.\nThe diagnostic sends only <code>initialize</code>, <code>initialized</code> and <code>config/read</code>, checks\nthe fixed baseline projection and disabled remote-control notice, and closes.\nIt accepts no account, model, prompt, configuration map or RPC selection and\nreturns no process, stream, credential or task-admission authority.</p>\n<p>One captured 60-second deadline bounds admission and native waits across both\nprocesses; the last 15 seconds are reserved for cleanup. Filesystem cleanup may\noutlast that deadline, with its raw promise still owned. Timeouts never prove closure.\nUnjoined cleanup retains the actual process owners, open lease database and\njournal in memory, with durable custody evidence in the returned private root.\nThe diagnostic does not automatically retry, release expired custody or remove\nthat state. Both native processes, their streams and protocol writes must join\nbefore lease release. Journal and database closure precede a successful returned\nor on-disk receipt. Its separate receipt always reports\n<code>productionQualified: false</code> and <code>network: &quot;denied&quot;</code>; it supplies lifecycle and\nconfiguration observations, not task execution, model-tool or auth-home isolation\nqualification. It is absent from default wiring and the public barrel.</p>\n<p>Account helpers and managed tasks share one fixed persistent configuration from\n<code>codex-managed-baseline.ts</code>. Its version-one bytes remain unchanged across tasks;\nthe native owner must refuse a different existing file instead of overwriting it.\nThe task session sends its selected model, service tier and instructions through\ndedicated <code>thread/start</code> fields. A closed, host-generated <code>config</code> overlay keeps\nall task capability denials and supplies a non-null reasoning effort. It accepts\nno caller configuration map. Non-null effort and tier also use explicit turn\noverrides; null values leave native defaults in place.</p>\n<p>The adapter defaults to unqualified. <code>Xcb.runTask()</code> refuses it before\naccount acquisition or process launch unless the trusted host supplies current\nqualification for the exact route, runtime and capability profile. Direct\nadapter calls also require qualification and a runtime-admitted request. Synthetic fixtures are not\nqualification evidence and do not enable the route in Textbutler or another app.</p>\n<p>The managed session checks ChatGPT account type, the public baseline configuration\nprojection and native thread settings before sending the task. <code>config/read</code>\nprecedes the thread overlay and does not prove the requested task selections.\nThose selections must match <code>ThreadStartResponse</code> before <code>turn/start</code>; a mismatch\nstops the session. The requested capability flags still require separate effective\ntool-inventory evidence. Its bounded callback ledger binds tool starts, broker calls,\nresults and completions to one thread and turn, rejects duplicates and unsupported\nnative operations, and records native token usage with unknown monetary cost.\nA turn-start settings notification is accepted only while that start request is\npending and must match the admitted thread controls. Repeated notifications must\nbe identical and remain bounded; resolved defaults are recorded only after the\nmatching reply, without changing the requested settings.\nCancellation revokes the broker and joins the process and admitted handlers;\nuncertain stop evidence retains account custody.</p>\n<p>An observed ChatGPT account does not distinguish native-managed storage from\nexternally supplied tokens. The trusted launcher must establish the authentication\nmode and configuration isolation independently. Likewise, dynamic tools add a\nbroker surface; they do not prove that built-in tools are absent. Callback\nfiltering cannot prevent an unobserved built-in operation. Session receipts\ntherefore report <code>productionQualified: false</code> and\n<code>exactToolInventoryObserved: false</code>. Live activation still requires evidence of\nthe effective tool inventory and host read/write confinement. The existing\nrelay-only process launcher's network policy is unchanged.</p>\n<p>On 2026-09-13, Codex <strong>0.154.0-alpha.6.2</strong> accepted the initial managed configuration\nand an empty ephemeral thread in separate native diagnostics with fresh private\nstate and network access denied. Configuration and thread-setting readback\npassed, and root exit, process-group absence and stdio closure were verified.\nThe diagnostics made no account, login or turn requests. They establish\nconfiguration and thread-response compatibility for that binary, not\nauthenticated execution or tool confinement.\nThose diagnostics predate the shared-baseline task overlay. The combined path\nstill requires native validation against the exact admitted runtime.</p>\n<p><code>codexManagedStaticCatalog({ model, catalog })</code> prepares a single-model static\ncatalog for a trusted native host. Supply public <code>ModelsResponse</code> metadata for\nthe exact runtime and select an exact model slug. The helper refuses duplicate\nslugs, missing models and non-JSON values, then returns a deeply immutable\nsnapshot, canonical JSON and its SHA-256. It preserves the selected model's\nprotocol and capability metadata while selecting direct tools and disabling\nshell, patch, experimental tools, search, experimental context, subagents and\nNode REPL. It performs no discovery, authentication or file I/O.</p>\n<p>The host owns the catalog file outside model-writable workspaces and binds its\ndigest to the selected executable, configuration and exact model. The catalog\nalone does not establish the effective tool inventory: native extensions can\nregister additional handlers. Managed configuration separately disables context,\ntoken-budget/history, time, deferred-execution and permission-request tools, and\nboth subagent switches. These declarative controls remain subject to exact-build\nnative validation and do not activate the managed subscription route.</p>\n<p>With a fixed <code>gpt-6-astra</code> catalog, local scripted-provider diagnostics on the\nsame binary verified empty and one-tool manifests in every request's Responses\nLite <code>additional_tools</code> input prefix. One permitted callback returned its exact\ntext result; nine forged built-in function calls each returned the exact\nunsupported-call response. Native process, stdio and listener joins passed, and\nan independent audit matched the retained binary, catalog, configuration and\nwire evidence. These no-authentication diagnostics leave managed sign-in,\nprovider egress and production profile qualification outstanding.</p>\n";
